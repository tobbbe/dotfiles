name: livity-app
root: ~/dev/livity-app/

windows:
  - editor:
      layout: even-vertical
      panes:
        - editor:
          - echo 'npx react-native run-ios --simulator="iPhone 15"'
        - server:
          - npm start
