name: xapp
root: ~/dev/xshapes-app/

windows:
  - editor:
      layout: even-vertical
      panes:
        - cli:
          - rni
        # - npm:
          # - replaceInConfig API2_ORIGIN "http:\/\/"$(localip)":3030" .env.development.local && xcrun simctl boot "iPhone 14 pro" && npm start
          # - npm start --reset-cache